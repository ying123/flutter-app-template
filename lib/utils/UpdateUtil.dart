class UpdateUtil {
  initialize () {

  }

  ///检查版本更新
  static Future<Null> checkUpdate({
    ///是否支持后台更新
    bool supportBackgroundUpdate = false,
    ///是否开启自动模式
    bool isAutoMode = false,}){

  }
}